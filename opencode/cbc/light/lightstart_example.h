#include <stdio.h>
#include"lightstart.h"

int main() 
{
	lightstart(5, 120.0);
	//Start optional light sensor calibration with the Light Sensor in Port #5, then end the program in 120.0 Seconds
	
	printf("Hello, World!\n");
	//Say Hi!
	return 0;
}
